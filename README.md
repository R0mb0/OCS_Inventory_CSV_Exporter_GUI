# OCS_Inventory_CSV_Exporter_GUI
Windows desktop app (Python + PySide6) that automates scheduled CSV exports from OCS Inventory NG, with encrypted credentials, automatic cleanup, and autostart after reboot.
